[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn web_app:app --host 0.0.0.0 --port $PORT --workers 1 --limit-concurrency 20 --timeout-keep-alive 5"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
